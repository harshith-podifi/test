# Todo app (greenfield)

This repository will host the application source for a Pod-managed greenfield **todo** product: API, persistence, web client, and observability will land here as implementation milestones complete.

## For readers and stakeholders

- **Today:** There is no application code on `main` yet; this README is the project’s public face until the first scaffold merges.
- **Architecture and context:** Living documentation for this project is maintained at the Pod workspace under `projects/test/docs/` (owned by the `pod-project-context` skill). Consult that tree for architecture-as-code and deeper technical detail.
- **Planning artifacts:** Use the links below to trace work from proposal through breakdown to the workspace spec that seeds context.

## Traceability

| Artifact | Location |
| --- | --- |
| Proposal | `20260512-todo-app.proposal` (workspace proposals tree) |
| Breakdown | `20260512-todo-app.breakdown.proposal` (workspace proposals tree) |
| Spec (doc-only seed) | `20260512-00-seed-project-context-docs.spec` (workspace specs tree) |

## Roadmap (high level)

1. Project context docs and workspace alignment (in progress at workspace level).
2. Backend scaffold, REST API, and JSON-backed store.
3. Single-page application (SPA).
4. Observability and hardening.

Questions or access to the workspace should go through your Pod workspace administrators.
